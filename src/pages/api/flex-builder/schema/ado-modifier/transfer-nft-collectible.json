{
  "schema": {
    "title": "Transfer NFT Collectible",
    "description": "Transfers ownership of a published token. Only available to the ADO owner, an approved operator or the purchaser in a TransferAgreement for the given ADO. Token ID # 3787sd78543",
    "$id": "/schemas/ado-modifier/transfer-nft-collectible",
    "type": "object",
    "required": ["token_id"],
    "properties": {
      "token_id": {
        "type": "string",
        "title": "Token ID"
      },
      "recipient address": {
        "type": "string",
        "title": "Recipient Address"
      }
    }
  },
  "ui-schema": {
    "token_id": { "ui:help": "the description of token id." },
    "recipient address": { "ui:help": "the description of recipient address." }
  },
  "form-data": {}
}
