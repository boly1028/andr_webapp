{
  "schema": {
    "title": "Update Lock",
    "description": "Sets a lock on the ADO. Preventing editing of any configuration fields. Applying to Splitter ADO #16312",
    "$id": "/schemas/ado-modifier/update-lock",
    "type": "object",
    "required": ["locked"],
    "properties": {
      "locked": {
        "type": "boolean",
        "title": "Locked"
      }
    }
  },
  "ui-schema": {},
  "form-data": {}
}
