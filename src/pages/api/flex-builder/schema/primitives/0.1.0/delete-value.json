{
  "schema": {
    "$schema": "primitives/0.1.0/delete-value",
    "class": "modifier",
    "classifier": "",
    "type": "object",
    "title": "Delete Value from Primitive",
    "description": "Deletes the data attached to the specified field name.",
    "required": ["delete_value"],
    "properties": {
      "delete_value": {
        "type": "object",
        "title": "Delete Value",
        "description": "If name is not specified the default field name (\"default\") will be used.",
        "properties": {
          "key": {
            "type": ["string", "null"],
            "title": "Field Name"
          }
        }
      }
    },
    "additionalProperties": false
  },
  "ui-schema": {},
  "form-data": {}
}
