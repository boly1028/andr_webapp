{
  "schema": {
    "title": "Update Owner",
    "description": "Update to a new owner.",
    "type": "object",
    "required": ["update_owner"],
    "properties": {
      "update_owner": {
        "type": "object",
        "required": ["address"],
        "properties": { "address": { "type": "string", "title": "address" } }
      }
    },
    "additionalProperties": false
  },
  "ui-schema": {},
  "form-data": {}
}
