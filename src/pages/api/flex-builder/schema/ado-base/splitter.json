{
  "schema": {
    "title": "Splitter",
    "description": "Set a percentage of funds to be distributed to specified Wallet Addresses.",
    "$id": "/schemas/ado-base/splitter",
    "type": "object",
    "required": ["percentage"],
    "properties": {
      "percentage": {
        "type": "string",
        "title": "Percentage",
        "maximum": 100
      },
      "address": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "title": "Alphanumeric Terra Address"
      }
    }
  },
  "ui-schema": {
    "percentage": { "ui:help": "The percentage to distribute to addresses." },
    "address": { "ui:help": "Address to distribute funds amongst." }
  },
  "form-data": {}
}
