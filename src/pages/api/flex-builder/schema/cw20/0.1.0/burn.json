{
  "schema": {
    "$id": "/schemas/cw20/burn",
    "class": "modifier",
    "classifier": "",
    "title": "Burn Tokens",
    "description": "Destroy tokens forever!",
    "type": "object",
    "required": ["burn"],
    "properties": {
      "burn": {
        "type": "object",
        "title": "Burn Tokens",
        "required": ["amount"],
        "properties": {
          "amount": {
            "type": "integer",
            "title": "Amount",
            "description": "The amount of tokens to be burned. !this process cannot be undone!",
            "format": "uint128",
            "minimum": 0.0
          }
        }
      }
    }
  },
  "ui-schema": {},
  "form-data": {}
}
