{
  "schema": {
    "$id": "/schemas/cw20/increase-allowance",
    "class": "modifier",
    "classifier": "",
    "title": "Increase Token Allowance",
    "description": "Move tokens to another account without triggering actions.",
    "type": "object",
    "required": ["transfer"],
    "properties": {
      "transfer": {
        "type": "object",
        "title": "Increase Token Allowance",
        "description": "Sets an amount of tokens from the owner that the specified spender can interact with. !If expires is Some(), overwrites current allowance expiration with this one.!",
        "required": ["amount", "recipient"],
        "properties": {
          "amount": {
            "type": "integer",
            "title": "Amount to Tokens",
            "description": "The amount of tokens to give the spender access to.",
            "format": "uint128",
            "minimum": 0.0
          },
          "recipient": {
            "type": "string",
            "title": "Spender",
            "description": "The address to receive the allowance."
          },
          "expiration-label": {
            "type": "object",
            "title": "Expiration Type",
            "description": "Expiration represents a point in time when some event happens. It can compare with a BlockInfo and will return is_expired() == true once the condition is hit (and for every block in the future) !This section needs to be moved to expiration once title/description display is resolved for type anyof/oneof!",
            "properties": {}
          },
          "Expiration": {
            "description": "Expiration represents a point in time when some event happens. It can compare with a BlockInfo and will return is_expired() == true once the condition is hit (and for every block in the future)",
            "oneOf": [
              {
                "title": "At Height",
                "description": "Will expire when `env.block.height` >= height",
                "type": "object",
                "required": ["at_height"],
                "properties": {
                  "at_height": {
                    "type": "integer",
                    "format": "uint64",
                    "title": "At Height",
                    "minimum": 0.0
                  }
                },
                "additionalProperties": false
              },
              {
                "title": "At Time",
                "description": "Will expire when `env.block.time` >= time",
                "type": "object",
                "required": ["at_time"],
                "properties": {
                  "at_time": {
                    "type": "string",
                    "format": "uint64",
                    "title": "At Time",
                    "description": "The time to expire when env.block.time. (As uint64)"
                  }
                },
                "additionalProperties": false
              },
              {
                "title": "Never",
                "description": "Will never expire.",
                "type": "object",
                "required": ["never"],
                "properties": {
                  "never": {
                    "type": "object"
                  }
                },
                "additionalProperties": false
              }
            ]
          }
        }
      }
    }
  },
  "ui-schema": {},
  "form-data": {}
}
