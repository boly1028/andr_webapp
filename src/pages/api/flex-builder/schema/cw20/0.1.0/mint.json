{
  "schema": {
    "$id": "/schemas/cw20/mint",
    "class": "modifier",
    "classifier": "",
    "title": "Mint Tokens",
    "description": "Only with the \"mint\" extension. If authorized, creates amount new tokens and adds to the recipient balance.",
    "type": "object",
    "required": ["mint"],
    "properties": {
      "mint": {
        "type": "object",
        "title": "Mint Tokens",
        "required": ["amount"],
        "properties": {
          "amount": {
            "type": "integer",
            "title": "Amount to Transfer",
            "description": "The amount of tokens to transfer.",
            "format": "uint128",
            "minimum": 0.0
          },
          "recipient": {
            "type": "string",
            "title": "Recipient of Transfer",
            "description": "The address to transfer the tokens to."
          }
        }
      }
    }
  },
  "ui-schema": {},
  "form-data": {}
}
