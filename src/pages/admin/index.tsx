import { Layout } from "@/modules/common";
import React, { FC } from "react"

interface Props {
}

const Page: FC<Props> = (props) => {
    const { } = props;
    return (
        <Layout>
        </Layout>
    )
}

export default Page