import { Heading } from "@chakra-ui/react";
import React, { memo } from "react";

const Loader: React.FC = memo(function Loader() {
  return <Heading>Loading...</Heading>;
});

export default Loader;
