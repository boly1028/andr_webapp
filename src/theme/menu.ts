const baseStyleList = {
  boxShadow: "lg",
  minW: "9rem",
  py: 0,
  borderRadius: "lg",
};

const baseStyleItem = {
  color: "primary.25",
  py: "0.7rem",
  fontSize: "sm",
  fontWeight: 500,
  _focus: {
    color: "primary.900",
  }
};

const baseStyle = {
  list: baseStyleList,
  item: baseStyleItem,
};

const styles = {
  baseStyle,
};

export default styles;
