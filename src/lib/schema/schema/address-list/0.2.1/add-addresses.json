{
  "schema": {
    "description": "Add multiple addresses to the address list",
    "type": "object",
    "required": ["add_addresses"],
    "properties": {
      "add_addresses": {
        "type": "object",
        "required": ["addresses"],
        "properties": {
          "addresses": {
            "type": "array",
            "items": { "type": "string" },
            "title": "Addresses"
          }
        },
        "additionalProperties": false,
        "title": "Add addresses"
      }
    },
    "additionalProperties": false,
    "title": "Add addresses",
    "$id": "add-addresses",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {},
  "form-data": {}
}
