{
  "schema": {
    "description": "Remove an address from the address list",
    "type": "object",
    "required": ["remove_address"],
    "properties": {
      "remove_address": {
        "type": "object",
        "required": ["address"],
        "properties": { "address": { "type": "string", "title": "Address" } },
        "additionalProperties": false,
        "title": "Remove address"
      }
    },
    "additionalProperties": false,
    "title": "Remove address",
    "$id": "remove-address",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "remove_address": {
      "ui:options": {
        "title": "Remove Address from List",
        "alerts": [
          {
            "text": "Only Owner / Operators can execute Remove Address.",
            "type": "warning"
          }
        ]
      },
      "address": {
        "ui:options": {
          "title": "Address to Remove",
          "description": "The address to remove from the list.",
          "help": ""
        }
      }
    }
  },
  "form-data": {}
}
