{
  "schema": {
    "type": "object",
    "required": ["sale_assets"],
    "properties": {
      "sale_assets": {
        "type": "object",
        "properties": {
          "limit": {
            "type": ["integer", "null"],
            "format": "uint32",
            "minimum": 0,
            "title": "Limit"
          },
          "start_after": { "type": ["string", "null"], "title": "Start after" }
        },
        "additionalProperties": false,
        "title": "Sale assets"
      }
    },
    "additionalProperties": false,
    "title": "Sale assets",
    "$id": "sale-assets",
    "class": "query",
    "version": "0.2.2",
    "classifier": ""
  },
  "ui-schema": {},
  "form-data": {}
}
