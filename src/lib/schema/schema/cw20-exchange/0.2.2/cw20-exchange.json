{
  "schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Cw20 exchange",
    "type": "object",
    "required": [
      "kernel_address",
      "token_address"
    ],
    "properties": {
      "kernel_address": {
        "type": "string",
        "title": "Kernel address",
        "default": ""
      },
      "modules": {
        "type": [
          "array",
          "null"
        ],
        "items": {
          "description": "A Module is a component that can be attached to an ADO, enhancing its functionality by adding specific features or capabilities, like keeping a list of addresses or adding a royalty rate.",
          "type": "object",
          "required": [
            "address",
            "is_mutable"
          ],
          "properties": {
            "address": {
              "description": "Can be any VFS referenced module address (located inside of compomemts in your Assets folder, under 'Address'), or app component name of a specific ADO module component within an app. (e.g. './rates').",
              "type": "string",
              "$original_type": "AndrAddr"
            },
            "is_mutable": {
              "type": "boolean"
            },
            "name": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false,
          "$original_type": "Module"
        },
        "title": "Modules"
      },
      "owner": {
        "type": [
          "string",
          "null"
        ],
        "title": "Owner"
      },
      "token_address": {
        "description": "Address of the CW20 token to be sold",
        "allOf": [
          {
            "description": "An address that can be used within the Andromeda ecosystem. Inspired by the cosmwasm-std `Addr` type. This address can be one of two things: 1. A valid human readable address e.g. `cosmos1...` 2. A valid Andromeda VFS path e.g. `/home/user/app/component`. VFS paths can be local in the case of an app and can be done by referencing `./component` they can also contain protocols for cross chain communication. A VFS path is usually structured as so: `<protocol>://<chain (required if ibc used)>/<path>` or `ibc://cosmoshub-4/user/app/component`",
            "type": "string",
            "$original_type": "AndrAddr"
          }
        ],
        "title": "Token address"
      }
    },
    "additionalProperties": false,
    "$id": "cw20-exchange",
    "version": "0.2.2",
    "classifier": "",
    "class": "baseADO",
    "description": "The CW20-Exchange ADO allows users to exchange CW20 tokens for native funds or other CW20s."
  },
  "ui-schema": {
    "token_address": {
      "ui:widget": "text",
      "ui:placeholder": "Enter Token Address",
      "ui:help": "Enter a valid CW20 token address. This can be a human-readable address (e.g., 'cosmos1...') or a valid Andromeda VFS path (e.g., '/home/user/app/component')."
    },
    "modules": {
      "ui:options": {
        "title": "Selected Modules",
        "description": "(Optional) The following Andromeda Modules can be attached to the contract: rates, address-list, and receipt."
      },
      "items": {
        "ui:options": { "title": "Module" },
        "ui:order": ["*", "is_mutable"],
        "address": {
          "ui:options": {
            "title": "Associated Modules",
            "description": "Can be any VFS module address found in your Assets folder, in every component, under 'Address'), or specific component name (e.g. './rates').",
            "alerts": [
              {
                "text": "Available modules: \"rates\", \"address-list\", and \"receipt\"",
                "type": "error"
              }
            ]
          },
          "identifier": {
            "ui:options": {
              "title": "Identifier",
              "description": "Can be either an address or app component name of a specific ADO within an app"
            }
          }
        },
        "module_type": {
          "ui:options": {
            "title": "Module Type",
            "description": "The name of the module to add. Can be set one of the following: \"rates\", \"address-list\",and \"receipt\"."
          }
        },
        "is_mutable": {
          "ui:options": {
            "title": "Is Module Able to be Modified?",
            "description": "Check the box if you want the module the be modifiable later."
          }
        }
      }
    },
    "kernel_address": { "ui:widget": "hidden" }
  },
  "form-data": {}
}
