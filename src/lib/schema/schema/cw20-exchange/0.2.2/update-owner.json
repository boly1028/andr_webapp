{
  "schema": {
    "type": "object",
    "required": ["update_owner"],
    "properties": {
      "update_owner": {
        "type": "object",
        "required": ["address"],
        "properties": { "address": { "type": "string", "title": "Address" } },
        "additionalProperties": false,
        "title": "Update owner"
      }
    },
    "additionalProperties": false,
    "title": "Update owner",
    "$id": "update-owner",
    "class": "modifier",
    "version": "0.2.2",
    "classifier": ""
  },
  "ui-schema": {
    "update_owner": {
      "ui:options": {
        "title": "Update Owner",
        "description": "Update the owner of the contract to a new address."
      },
      "address": {
        "ui:widget": "text",
        "ui:title": "New Owner Address",
        "ui:placeholder": "e.g., andr1...",
        "ui:description": "Enter the new owner address here."
      }
    }
  },
  "form-data": {}
}