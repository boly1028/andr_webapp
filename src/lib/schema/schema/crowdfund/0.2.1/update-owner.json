{
  "schema": {
    "type": "object",
    "required": ["update_owner"],
    "properties": {
      "update_owner": {
        "type": "object",
        "required": ["address"],
        "properties": { 
          "address": { 
            "type": "string", 
            "title": "Address" 
          } 
        },
        "additionalProperties": false,
        "title": "Update owner"
      }
    },
    "additionalProperties": false,
    "title": "Update owner",
    "$id": "update-owner",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "update_owner": {
      "address": {
        "ui:widget": "text",
        "ui:placeholder": "Enter new owner address",
        "ui:help": "Input the address that will become the new owner."
      }
    }
  },
  "form-data": {}
}
