{
  "schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Crowdfund",
    "type": "object",
    "required": ["can_mint_after_sale", "kernel_address", "token_address"],
    "properties": {
      "can_mint_after_sale": {
        "type": "boolean",
        "title": "Can mint after sale"
      },
      "kernel_address": {
        "type": "string",
        "title": "Kernel address",
        "default": ""
      },
      "modules": {
        "type": ["array", "null"],
        "items": {
          "description": "A Module is a component that can be attached to an ADO, enhancing its functionality by adding specific features or capabilities, like keeping a list of addresses or adding a royalty rate.",
          "type": "object",
          "required": ["address", "is_mutable"],
          "properties": {
            "address": {
              "description": "An address that can be used within the Andromeda ecosystem. ",
              "type": "string",
              "$original_type": "AndrAddr"
            },
            "is_mutable": { "type": "boolean" },
            "name": { "type": ["string", "null"] }
          },
          "additionalProperties": false,
          "$original_type": "Module"
        },
        "title": "Modules"
      },
      "owner": { "type": ["string", "null"], "title": "Owner" },
      "token_address": {
        "description": "An address that can be used within the Andromeda ecosystem. ",
        "type": "string",
        "$original_type": "AndrAddr",
        "title": "Token address"
      }
    },
    "additionalProperties": false,
    "$id": "crowdfund",
    "version": "0.2.1",
    "classifier": "sale",
    "class": "baseADO",
    "description": "An ADO for initiating a token crowdfund, with a minting feature included."
  },
  "ui-schema": {
    "ui:order": ["token_address", "modules", "can_mint_after_sale", "*"],
    "can_mint_after_sale": {
      "ui:options": {
        "title": "Allow minting after sale?",
        "description": "Tick the box if you want to allow minting after a sale has completed.",
        "alerts": [
          {
            "text": "Minting is not allowed while a sale is in progress.",
            "type": "warning"
          }
        ]
      }
    },
    "modules": {
      "ui:options": {
        "title": "Add Modules",
        "description": "(Optional) Add attachable Andromeda ADO Modules to the contract. Currently compatible: 'rates', 'address-list', and 'receipt'",
        "placeholder": "e.g., rates"
      },
      "items": {
        "ui:options": { "title": "Module" },
        "ui:order": ["*", "is_mutable"],
        "address": {
          "ui:options": {
            "title": "Add Module",
            "description": "Only allowed modules. Can be either an address or a component name of a specific ADO within an app",
            "alerts": [
              {
                "text": "Allowed modules: rates, address-list, and receipt.",
                "type": "error"
              }
            ],
            "placeholder": "e.g., rates"
          },
          "identifier": {
            "ui:options": {
              "title": "Identifier",
              "description": "Can be either an address or app component name of a specific ADO within an app.",
              "placeholder": "e.g., ./componentName"
            }
          }
        },
        "module_type": {
          "ui:options": {
            "title": "Module Type",
            "description": "Name of the module to add. Can be set one of the following: rates, address-list.",
            "placeholder": "e.g., rates"
          }
        },
        "is_mutable": {
          "ui:options": {
            "title": "Is Module Able to be Modified?",
            "description": "Tick the box if you want the module to be modifiable later."
          }
        }
      }
    },
    "token_address": {
      "ui:options": {
        "title": "Token Address",
        "description": "The contract address of the token.",
        "placeholder": "e.g., andr1xxx...asdf"
      },
      "identifier": {
        "ui:options": {
          "title": "Identifier",
          "description": " ",
          "help": "Can be either an address or app component name of a specific ADO within an app",
          "placeholder": "e.g., ./componentName"
        }
      }
    },
    "kernel_address": { "ui:widget": "hidden" }
  },
  "form-data": {}
  }
  