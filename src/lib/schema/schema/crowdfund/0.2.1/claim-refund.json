{
  "schema": {
    "description": "Allow a user to claim their own refund if the minimum number of tokens are not sold.",
    "type": "object",
    "required": ["claim_refund"],
    "properties": {
      "claim_refund": {
        "type": "object",
        "additionalProperties": false,
        "title": "Claim refund"
      }
    },
    "additionalProperties": false,
    "title": "Claim refund",
    "$id": "claim-refund",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "claim_refund": {
      "ui:options": {
        "title": "Claim Refund",
        "description": "Claim your refund if the sale did not meet the minimum token requirement.",
        "placeholder": "Claim your refund"
      }
    }
  },
  "form-data": {}
}

