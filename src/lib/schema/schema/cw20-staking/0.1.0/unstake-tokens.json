{
    "schema": {
        "title": "Unstake Tokens",
        "description": "Unstakes the specified amount of assets, or all if not specified. The user's pending rewards and indexes are updated for each additional reward token.",
        "type": "object",
        "required": [
            "unstake_tokens"
        ],
        "properties": {
            "unstake_tokens": {
                "type": "object",
                "properties": {
                    "amount": {
                        "title": "Unstake Amount",
                        "anyOf": [
                            {
                                "title": "Unstake Amount",
                                "description": "Defaults to the maximum amount.",
                                "type": "string"
                            },
                            {
                                "title": "No Amount",
                                "type": "null"
                            }
                        ]
                    }
                }
            }
        },
        "additionalProperties": false,
        "$id": "unstake-tokens",
        "class": "modifier",
        "version": "0.1.0",
        "classifier": ""
    },
    "ui-schema": {
        "unstake_tokens": {
            "ui:options": {
                "title": "Unstake Tokens",
                "description": " "
            },
            "amount": {}
        }
    },
    "form-data": {}
}