{
  "schema": {
    "type": "object",
    "required": ["claim_ownership"],
    "properties": {
      "claim_ownership": {
        "type": "object",
        "properties": {
          "name": { "type": ["string", "null"], "title": "Name" }
        },
        "additionalProperties": false,
        "title": "Claim ownership"
      }
    },
    "additionalProperties": false,
    "title": "Claim ownership",
    "$id": "claim-ownership",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "claim_ownership": {
      "name": {
        "ui:widget": "text",
        "ui:title": "Owner's Name",
        "ui:placeholder": "e.g., andr1...",
        "ui:description": "Enter the address of the new owner (optional)."
      }
    }
  },
  "form-data": {}
}
