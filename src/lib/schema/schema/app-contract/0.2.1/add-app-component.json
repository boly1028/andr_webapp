{
  "schema": {
    "type": "object",
    "required": ["add_app_component"],
    "properties": {
      "add_app_component": {
        "type": "object",
        "required": ["component"],
        "properties": {
          "component": {
            "type": "object",
            "required": ["ado_type", "instantiate_msg", "name"],
            "properties": {
              "ado_type": { "type": "string" },
              "instantiate_msg": {
                "description": "Binary is a wrapper around Vec<u8> to add base64 de/serialization with serde. ",
                "type": "string",
                "$original_type": "Binary"
              },
              "name": { "type": "string" }
            },
            "additionalProperties": false,
            "$original_type": "AppComponent",
            "title": "Component"
          }
        },
        "additionalProperties": false,
        "title": "Add app component"
      }
    },
    "additionalProperties": false,
    "title": "Add app component",
    "$id": "add-app-component",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "add_app_component": {
      "component": {
        "ado_type": {
          "ui:widget": "text",
          "ui:title": "ADO Type",
          "ui:placeholder": "cw721"
        },
        "instantiate_msg": {
          "ui:widget": "textarea",
          "ui:title": "ADO Selection",
          "ui:placeholder": "This field will autopopulate as you choose and configure your ADO",
          "ui:description": "Select an ADO from the dropdown list. Make sure it matches what you entered into the 'ado_type' field above."
        },
        "name": {
          "ui:widget": "text",
          "ui:title": "Component Name",
          "ui:placeholder": "NameOfThisComponent"
        }
      }
    }
  },
  "form-data": {}
}
