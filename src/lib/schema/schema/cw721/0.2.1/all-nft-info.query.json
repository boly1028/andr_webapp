{
  "schema": {
    "description": "The data of a token and any approvals assigned to it",
    "type": "object",
    "required": ["all_nft_info"],
    "properties": {
      "all_nft_info": {
        "type": "object",
        "required": ["token_id"],
        "properties": {
          "include_expired": {
            "type": ["boolean", "null"],
            "title": "Include expired"
          },
          "token_id": {
            "type": "string",
            "title": "Token id"
          }
        },
        "additionalProperties": false,
        "title": "All nft info"
      }
    },
    "additionalProperties": false,
    "title": "All nft info",
    "$id": "all-nft-info",
    "class": "query",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "all_nft_info": {
      "ui:options": {
        "title": "NFT Information",
        "description": "Retrieve detailed information about a specific token, including any approvals assigned to it."
      },
      "token_id": {
        "ui:options": {
          "title": "Token ID",
          "description": "Enter the unique identifier of the token to retrieve its information.",
          "placeholder": "e.g., unique-token-id-123"
        }
      },
      "include_expired": {
        "ui:options": {
          "title": "Include Expired Approvals",
          "description": "Select whether to include expired approvals in the token information."
        }
      }
    }
  },
  "form-data": {}
}
