{
  "schema": {
    "type": "object",
    "required": ["update_owner"],
    "properties": {
      "update_owner": {
        "type": "object",
        "required": ["address"],
        "properties": { "address": { "type": "string", "title": "Address" } },
        "additionalProperties": false,
        "title": "Update owner"
      }
    },
    "additionalProperties": false,
    "title": "Update owner",
    "$id": "update-owner",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "update_owner": {
      "ui:options": {
        "title": "Update Owner",
        "description": "Modify the owner of the contract. Only the current owner can perform this action."
      },
      "address": {
        "ui:options": {
          "title": "New Owner Address",
          "description": "Enter the address of the new owner. This action will transfer all ownership rights to the specified address.",
          "placeholder": "e.g., andr1fl48v...0eh"
        }
      }
    }
  },
  "form-data": {}
}
