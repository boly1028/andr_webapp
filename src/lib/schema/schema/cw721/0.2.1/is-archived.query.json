{
  "schema": {
    "description": "If the token is archived",
    "type": "object",
    "required": ["is_archived"],
    "properties": {
      "is_archived": {
        "type": "object",
        "required": ["token_id"],
        "properties": { "token_id": { "type": "string", "title": "Token id" } },
        "additionalProperties": false,
        "title": "Is archived"
      }
    },
    "additionalProperties": false,
    "title": "Is archived",
    "$id": "is-archived",
    "class": "query",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {},
  "form-data": {}
}
