{
  "schema": {
    "type": "object",
    "required": ["balance"],
    "properties": {
      "balance": {
        "type": "object",
        "required": ["address"],
        "properties": {
          "address": {
            "description": "An address that can be used within the Andromeda ecosystem. Inspired by the cosmwasm-std `Addr` type.",
            "type": "string",
            "$original_type": "AndrAddr",
            "title": "Address"
          }
        },
        "additionalProperties": false,
        "title": "Balance"
      }
    },
    "additionalProperties": false,
    "title": "Balance",
    "$id": "balance",
    "class": "query",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "balance": {
      "address": {
        "ui:options": {
          "title": "Address for Balance Query",
          "description": "Enter the Andromeda address to query its balance. This can be a human readable address or an Andromeda VFS path.",
          "placeholder": "e.g., andr1fl48v...0eh or ./componentName"
        }
      }
    }
  },
  "form-data": {}
}
