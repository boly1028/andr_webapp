{
  "schema": {
    "description": "Transfers ownership of a token",
    "type": "object",
    "required": ["transfer_nft"],
    "properties": {
      "transfer_nft": {
        "type": "object",
        "required": ["recipient", "token_id"],
        "properties": {
          "recipient": { "type": "string", "title": "Recipient" },
          "token_id": { "type": "string", "title": "Token id" }
        },
        "additionalProperties": false,
        "title": "Transfer nft"
      }
    },
    "additionalProperties": false,
    "title": "Transfer nft",
    "$id": "transfer-nft",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "transfer_nft": {
      "recipient": {
        "ui:placeholder": "e.g., andr1fl48v...0eh",
        "ui:description": "Address of the new owner to which the token will be transferred."
      },
      "token_id": {
        "ui:placeholder": "NFT-1",
        "ui:description": "The unique identifier for the token being transferred."
      }
    }
  },
  "form-data": {}
}
