{
  "schema": {
    "description": "Amount of tokens minted by the contract",
    "type": "object",
    "required": ["num_tokens"],
    "properties": {
      "num_tokens": {
        "type": "object",
        "additionalProperties": false,
        "title": "Num tokens"
      }
    },
    "additionalProperties": false,
    "title": "Num tokens",
    "$id": "num-tokens",
    "class": "query",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "num_tokens": {
      "ui:options": {
        "title": "Number of Tokens",
        "description": "Displays the total amount of tokens that have been minted by this contract."
      }
    }
  },
  "form-data": {}
}
