{
  "schema": {
    "description": "Find out who owns the currently selected contract.",
    "type": "object",
    "required": ["owner"],
    "properties": {
      "owner": {
        "type": "object",
        "additionalProperties": false,
        "title": "Owner"
      }
    },
    "additionalProperties": false,
    "title": "Owner",
    "$id": "owner",
    "class": "query",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "owner": {
      "ui:options": {
        "title": "Contract Ownership Query",
        "description": "Click 'Query' to find out who owns the currently selected contract. This action will retrieve the owner's address."
      }
    }
  },
  "form-data": {}
}
