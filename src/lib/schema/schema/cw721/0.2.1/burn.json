{
  "schema": {
    "description": "Burns a token, removing all data related to it. The ID of the token is still reserved.",
    "type": "object",
    "required": ["burn"],
    "properties": {
      "burn": {
        "type": "object",
        "required": ["token_id"],
        "properties": { "token_id": { "type": "string", "title": "Token id" } },
        "additionalProperties": false,
        "title": "Burn"
      }
    },
    "additionalProperties": false,
    "title": "Burn",
    "$id": "burn",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "burn": {
      "ui:options": {
        "title": "Burn Token",
        "description": "Permanently remove a token from the contract. This action is irreversible."
      },
      "token_id": {
        "ui:options": {
          "title": "Token ID",
          "description": "Enter the ID of the token you wish to burn. Ensure the ID is correct as this action cannot be undone.",
          "placeholder": "e.g., Token123"
        }
      }
    }
  },
  "form-data": {}
}
