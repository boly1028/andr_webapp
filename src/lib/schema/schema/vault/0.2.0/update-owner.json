{
  "schema": {
    "type": "object",
    "required": ["update_owner"],
    "properties": {
      "update_owner": {
        "type": "object",
        "required": ["address"],
        "properties": { "address": { "type": "string", "title": "Address" } },
        "additionalProperties": false,
        "title": "Update owner"
      }
    },
    "additionalProperties": false,
    "title": "Update owner",
    "$id": "update-owner",
    "class": "modifier",
    "version": "0.2.0",
    "classifier": ""
  },
  "ui-schema": {
    "update_owner": {
      "ui:options": {
        "title": "Owner Update",
        "description": "Update the owner by specifying a new address."
      },
      "address": {
        "ui:options": {
          "title": "New Owner Address",
          "description": "Enter the address that will become the new owner.",
          "placeholder": "e.g., andr1... or /home/user/app/component"
        }
      }
    }
  },
  "form-data": {}
}
