{
  "schema": {
    "type": "object",
    "required": ["is_operator"],
    "properties": {
      "is_operator": {
        "type": "object",
        "required": ["address"],
        "properties": { "address": { "type": "string", "title": "Address" } },
        "additionalProperties": false,
        "title": "Is operator"
      }
    },
    "additionalProperties": false,
    "title": "Is operator",
    "$id": "is-operator",
    "class": "query",
    "version": "0.2.0",
    "classifier": ""
  },
  "ui-schema": {},
  "form-data": {}
}
