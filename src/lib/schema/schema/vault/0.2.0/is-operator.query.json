{
  "schema": {
    "type": "object",
    "required": ["is_operator"],
    "properties": {
      "is_operator": {
        "type": "object",
        "required": ["address"],
        "properties": { "address": { "type": "string", "title": "Address" } },
        "additionalProperties": false,
        "title": "Is operator"
      }
    },
    "additionalProperties": false,
    "title": "Is operator",
    "$id": "is-operator",
    "class": "query",
    "version": "0.2.0",
    "classifier": ""
  },
  "ui-schema": {
    "is_operator": {
      "address": {
        "ui:widget": "text",
        "ui:description": "Enter contract address",
        "ui:placeholder": "e.g., andr1... or ./componentName",
        "ui:help": "Enter a valid address to check its balance. The address should be in the format recognized by the Andromeda ecosystem.",
        "ui:options": {
          "inputType": "text"
        }
      }
    }
  },
  "form-data": {}
}
 