{
  "schema": {
    "type": "object",
    "required": ["vault_balance"],
    "properties": {
      "vault_balance": {
        "type": "object",
        "required": ["address"],
        "properties": {
          "address": {
            "description": "An address that can be used within the Andromeda ecosystem. Inspired by the cosmwasm-std `Addr` type. https://github.com/CosmWasm/cosmwasm/blob/2a1c698520a1aacedfe3f4803b0d7d653892217a/packages/std/src/addresses.rs#L33\n\nThis address can be one of two things: 1. A valid human readable address e.g. `cosmos1...` 2. A valid Andromeda VFS path e.g. `/home/user/app/component`\n\nVFS paths can be local in the case of an app and can be done by referencing `./component` they can also contain protocols for cross chain communication. A VFS path is usually structured as so:\n\n`<protocol>://<chain (required if ibc used)>/<path>` or `ibc://cosmoshub-4/user/app/component`",
            "type": "string",
            "$original_type": "AndrAddr",
            "title": "Address"
          },
          "denom": { "type": ["string", "null"], "title": "Denom" },
          "strategy": {
            "anyOf": [
              {
                "type": "string",
                "enum": ["anchor"],
                "$original_type": "StrategyType"
              },
              { "type": "null" }
            ],
            "title": "Strategy"
          }
        },
        "additionalProperties": false,
        "title": "Vault balance"
      }
    },
    "additionalProperties": false,
    "title": "Vault balance",
    "$id": "vault-balance",
    "class": "query",
    "version": "0.2.0",
    "classifier": ""
  },
  "ui-schema": {
    "vault_balance": {
      "ui:options": {
        "title": "Vault Balance Query",
        "description": "Retrieve the balance details of a specific vault."
      },
      "address": {
        "ui:options": {
          "title": "Address",
          "description": "Specify the vault's Andromeda ecosystem address or a VFS path which you wish to query.",
          "placeholder": "e.g., cosmos1... or ./componentName"
        }
      },
      "denom": {
        "ui:options": {
          "title": "Denomination",
          "description": "Specify the denomination of the assets in the vault (optional).",
          "placeholder": "e.g., ANDR"
        }
      },
      "strategy": {
        "ui:options": {
          "title": "Strategy",
          "description": "Select the strategy associated with the vault (optional).",
          "optionNames": ["Strategy", "None"]
        }
      }
    }
  },
  "form-data": {}
}
