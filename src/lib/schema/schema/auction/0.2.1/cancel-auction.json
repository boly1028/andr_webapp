{
  "schema": {
    "type": "object",
    "required": ["cancel_auction"],
    "properties": {
      "cancel_auction": {
        "type": "object",
        "required": ["token_address", "token_id"],
        "properties": {
          "token_address": { "type": "string", "title": "Token address" },
          "token_id": { "type": "string", "title": "Token id" }
        },
        "additionalProperties": false,
        "title": "Cancel auction"
      }
    },
    "additionalProperties": false,
    "title": "Cancel auction",
    "$id": "cancel-auction",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "cancel_auction": {
      "token_address": {
        "ui:widget": "text",
        "ui:placeholder": "Enter the token's address",
        "ui:help": "The address of the token for which the auction is being canceled."
      },
      "token_id": {
        "ui:widget": "text",
        "ui:placeholder": "Enter the token's ID",
        "ui:help": "The unique identifier of the token for which the auction is being canceled."
      }
    }
  },
  "form-data": {}
}
