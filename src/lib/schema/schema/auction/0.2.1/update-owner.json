{
  "schema": {
    "type": "object",
    "required": ["update_owner"],
    "properties": {
      "update_owner": {
        "type": "object",
        "required": ["address"],
        "properties": { "address": { "type": "string", "title": "Address" } },
        "additionalProperties": false,
        "title": "Update owner"
      }
    },
    "additionalProperties": false,
    "title": "Update owner",
    "$id": "update-owner",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "update_owner": {
      "address": {
        "ui:widget": "text",
        "ui:placeholder": "e.g., andr1...95rj",        
        "ui:description": "This address can be your wallet, or can be another contract that you wish to declare as the owner of this auction."
      }
    }
  },
  "form-data": {}
}
