{
  "schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Auction",
    "type": "object",
    "required": ["kernel_address"],
    "properties": {
      "kernel_address": {
        "type": "string",
        "title": "Kernel address",
        "default": ""
      },
      "modules": {
        "type": ["array", "null"],
        "items": {
          "description": "A Module is a component that can be attached to an ADO, enhancing its functionality by adding specific features or capabilities, like keeping a list of addresses or adding a royalty rate.",
          "type": "object",
          "required": ["address", "is_mutable"],
          "properties": {
            "address": {
              "description": "Can be either an address or app component name of a specific ADO within an app",
              "type": "string",
              "$original_type": "AndrAddr"
            },
            "is_mutable": { "type": "boolean" },
            "name": { "type": ["string", "null"] }
          },
          "additionalProperties": false,
          "$original_type": "Module"
        },
        "title": "Modules"
      },
      "owner": { "type": ["string", "null"], "title": "Owner" }
    },
    "additionalProperties": false,
    "$id": "auction",
    "version": "0.2.1",
    "classifier": "sale",
    "class": "baseADO",
    "description": "The Auction ADO enables users to auction off items."
  },
  "ui-schema": {
    "modules": {
      "ui:options": {
        "title": "Selected Modules",
        "description": "(Optional) Add attachable Andromeda ADO Modules to the contract. Currently compatible: 'rates', 'address-list', and 'receipt'"
      },
      "items": {
        "ui:options": { "title": "Module" },
        "ui:order": ["*", "is_mutable"],
        "address": {
          "ui:options": {
            "title": "Add Module",
            "description": "Only allowed modules. Can be either an address or a component name of a specific ADO within an app",
            "alerts": [
              {
                "text": "Allowed modules: rates, address-list, and receipt.",
                "type": "error"
              }
            ]
          },
          "identifier": {
            "ui:options": {
              "title": "Identifier",
              "description": "Can be either an address or a component name of a specific ADO within an app."
            }
          }
        },
        "module_type": {
          "ui:options": {
            "title": "Module Type",
            "description": "The name of the module to add. Can be set one of the following: rates, address-list, and receipt."
          }
        },
        "is_mutable": {
          "ui:options": {
            "title": "Is the Module Able to be Modified?",
            "description": "If the field is checked then the module is allowed to be modified."
          }
        }
      }
    },
    "kernel_address": { "ui:widget": "hidden" }
  },
  "form-data": {}
}
