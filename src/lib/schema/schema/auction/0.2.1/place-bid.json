{
  "schema": {
    "description": "Places a bid on the current auction for the given token_id.",
    "type": "object",
    "required": ["place_bid"],
    "properties": {
      "place_bid": {
        "type": "object",
        "required": ["token_address", "token_id", "bid_amount"],
        "properties": {
          "token_address": { "type": "string", "title": "Token address" },
          "token_id": { "type": "string", "title": "Token id" },
          "bid_amount": {
            "type": "string",
            "title": "Bid Amount"
          }
        },
        "additionalProperties": false,
        "title": "Place bid"
      }
    },
    "additionalProperties": false,
    "title": "Place bid",
    "$id": "place-bid",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "place_bid": {
      "ui:options": {
        "alerts": [
          {
            "text": "Bids cannot be made by the auction owner.",
            "type": "error"
          },
          {
            "text": "Bids cannot be placed if you are currently the highest bidder.",
            "type": "error"
          }
        ]
      },
      "token_address": {
        "ui:widget": "text",
        "ui:title": "Token Address",
        "ui:placeholder": "e.g., andr1...5rj",
        "ui:description": "The address of the NFT collection containing the token you want to bid on."
      },
      "token_id": {
        "ui:widget": "text",
        "ui:title": "Token ID",
        "ui:placeholder": "e.g., YourTokenName",
        "ui:description": "The unique identifier of the token you want to bid on."
      },
      "bid_amount": {
        "ui:widget": "text",
        "ui:title": "Bid Amount",
        "ui:placeholder": "e.g., 100",
        "ui:description": "The amount you want to bid for the token."
      }
    }
  },
  "form-data": {}
}
