{
  "schema": {
    "description": "Transfers the given token to the auction winner's address once the auction is over.",
    "type": "object",
    "required": ["claim"],
    "properties": {
      "claim": {
        "type": "object",
        "required": ["token_address", "token_id"],
        "properties": {
          "token_address": { "type": "string", "title": "Token address" },
          "token_id": { "type": "string", "title": "Token id" }
        },
        "additionalProperties": false,
        "title": "Claim"
      }
    },
    "additionalProperties": false,
    "title": "Claim",
    "$id": "claim",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "claim": {
      "ui:order": ["token_address", "token_id", "*"],
      "ui:options": {
        "title": "Claim Token",
        "description": "Complete the claim process to transfer the token to the auction winner."
      },
      "token_address": {
        "ui:widget": "text",
        "ui:placeholder": "e.g., andr1fl48v...0eh",
        "ui:options": {
          "title": "Token Address",
          "description": "The blockchain address of the token."
        }
      },
      "token_id": {
        "ui:widget": "text",
        "ui:placeholder": "e.g., 100",
        "ui:options": {
          "title": "Token ID",
          "description": "The unique identifier for the token."
        }
      }
    }
  },
  "form-data": {}
}
