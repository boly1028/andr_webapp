{
  "schema": {
    "type": "object",
    "required": ["merkle_root"],
    "properties": {
      "merkle_root": {
        "type": "object",
        "required": ["stage"],
        "properties": {
          "stage": {
            "type": "integer",
            "format": "uint8",
            "minimum": 0,
            "title": "Stage"
          }
        },
        "additionalProperties": false,
        "title": "Merkle root"
      }
    },
    "additionalProperties": false,
    "title": "Merkle root",
    "$id": "merkle-root",
    "class": "query",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {},
  "form-data": {}
}
