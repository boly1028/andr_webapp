{
  "schema": {
    "type": "object",
    "required": ["update_owner"],
    "properties": {
      "update_owner": {
        "type": "object",
        "required": ["address"],
        "properties": { "address": { "type": "string", "title": "Address" } },
        "additionalProperties": false,
        "title": "Update owner"
      }
    },
    "additionalProperties": false,
    "title": "Update owner",
    "$id": "update-owner",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "update_owner": {
      "ui:options": {
        "title": "Owner Update",
        "description": "Update the owner address of the contract."
      },
      "address": {
        "ui:options": {
          "title": "New Owner Address",
          "description": "Enter the new address to assign as the owner.",
          "placeholder": "andr1..."
        }
      }
    }
  },
  "form-data": {}
}
