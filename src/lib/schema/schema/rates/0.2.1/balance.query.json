{
  "schema": {
    "type": "object",
    "required": ["balance"],
    "properties": {
      "balance": {
        "type": "object",
        "required": ["address"],
        "properties": {
          "address": {
            "description": "An address that can be used within the Andromeda ecosystem. Inspired by the cosmwasm-std `Addr` type.",
            "type": "string",
            "$original_type": "AndrAddr",
            "title": "Address"
          }
        },
        "additionalProperties": false,
        "title": "Balance"
      }
    },
    "additionalProperties": false,
    "title": "Balance",
    "$id": "balance",
    "class": "query",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "balance": {
      "address": {
        "ui:widget": "text",
        "ui:title": "Address",
        "ui:placeholder": "e.g., andr1... or ./componentName",
        "ui:description": "Enter a valid Andromeda wallet address or VFS path. Example: andr1... or ./componentName"
      }
    }
  },
  "form-data": {}
}
