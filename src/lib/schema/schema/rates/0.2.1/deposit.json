{
  "schema": {
    "type": "object",
    "required": ["deposit"],
    "properties": {
      "deposit": {
        "type": "object",
        "properties": {
          "msg": {
            "anyOf": [
              {
                "description": "Binary is a wrapper around Vec<u8> to add base64 de/serialization with serde. .",
                "type": "string",
                "$original_type": "Binary",
                "title": "Message"
              },
              { "type": "null" }
            ]
          },
          "recipient": {
            "anyOf": [
              {
                "description": "An address that can be used within the Andromeda ecosystem. `",
                "type": "string",
                "$original_type": "AndrAddr",
                "title": "Recipient Address"
              },
              { "type": "null" }
            ]
          }
        },
        "additionalProperties": false,
        "title": "Deposit"
      }
    },
    "additionalProperties": false,
    "title": "Deposit",
    "$id": "deposit",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "deposit": {
      "msg": {
        "ui:title": "Message",
        "ui:placeholder": "This field will autopopulate",
        "ui:description": "Binary message as a wrapper around Vec<u8>."
      },
      "recipient": {
        "ui:title": "Recipient Address",
        "ui:placeholder": "e.g., andr1... or ./componentName",
        "ui:description": "Enter a valid Andromeda recipient address or VFS path. Example: andr1... or ./component"
      }
    }
  },
  "form-data": {}
}
