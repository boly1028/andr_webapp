{
  "schema": {
    "description": "Transfer tokens to an ADO and trigger an action on the receiving ADO.",
    "type": "object",
    "required": ["send"],
    "properties": {
      "send": {
        "type": "object",
        "required": ["amount", "contract", "msg"],
        "properties": {
          "amount": {
            "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding...",
            "type": "string",
            "$original_type": "Uint128",
            "title": "Amount"
          },
          "contract": {
            "type": "string",
            "title": "Contract"
          },
          "msg": {
            "description": "Binary is a wrapper around Vec<u8> to add base64 de/serialization...",
            "type": "string",
            "$original_type": "Binary",
            "title": "Msg"
          }
        },
        "additionalProperties": false,
        "title": "Send"
      }
    },
    "additionalProperties": false,
    "title": "Send",
    "$id": "send",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "send": {
      "ui:order": ["contract", "amount", "msg", "*"],
      "ui:options": {
        "title": "Send Tokens",
        "description": "Transfer tokens to a specified contract address and trigger an associated action."
      },
      "amount": {
        "ui:widget": "text",
        "ui:placeholder": "e.g., 10000",
        "ui:options": {
          "title": "Amount Being Sent",
          "description": "Specify the amount of tokens to send. Ensure the format aligns with Uint128 standards.",
          "alerts": [
            {
              "text": "The amount sent might be affected depending on the attached modules.",
              "type": "warning"
            }
          ]
        }
      },
      "contract": {
        "ui:widget": "text",
        "ui:placeholder": "e.g., andr1fl48v...0eh",
        "ui:options": {
          "title": "Receiving Contract Address",
          "description": "Enter the address of the contract that will receive the tokens."
        }
      },
      "msg": {
        "ui:widget": "cw20ReceiveMsg",
        "ui:placeholder": "note: This field autopopulates when you select and configure a message.",
        "ui:options": {
          "title": "Supporting Message",
          "description": "Select an option from the dropdowns, then enter data or details for the sale."
        }
      }
    }
  },
  "form-data": {}
}
