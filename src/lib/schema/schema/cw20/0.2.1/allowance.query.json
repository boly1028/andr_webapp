{
  "schema": {
    "description": "Only with \"allowance\" extension. Returns how much spender can use from owner account, 0 if unset. Return type: AllowanceResponse.",
    "type": "object",
    "required": ["allowance"],
    "properties": {
      "allowance": {
        "type": "object",
        "required": ["owner", "spender"],
        "properties": {
          "owner": { "type": "string", "title": "Owner" },
          "spender": { "type": "string", "title": "Spender" }
        },
        "additionalProperties": false,
        "title": "Allowance"
      }
    },
    "additionalProperties": false,
    "title": "Allowance",
    "$id": "allowance",
    "class": "query",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "ui:description":"Only with \"allowance\" extension. Returns how much spender can use from owner account.",
    "allowance": {
      "ui:options": {
        "title": "Query Allowance",
        "description": "Check the amount a spender is authorized to use from the owner's account."
      },
      "owner": {
        "ui:options": {
          "title": "Owner Address",
          "description": "The wallet address of the owner of the funds.",
          "placeholder": "e.g., andr1fl48v...0eh",
          "help": "Enter the address of the account owner."
        }
      },
      "spender": {
        "ui:options": {
          "title": "Spender Address",
          "description": "The wallet address of the spender who is authorized to use the funds.",
          "placeholder": "e.g., andr1spender...0eh",
          "help": "Enter the address of the authorized spender."
        }
      }
    }
  },
  "form-data": {}
}
