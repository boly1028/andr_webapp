{
  "schema": {
    "description": "Only with \"mintable\" extension. Returns who can mint and the hard cap on maximum tokens after minting. Return type: MinterResponse.",
    "type": "object",
    "required": ["minter"],
    "properties": {
      "minter": {
        "type": "object",
        "additionalProperties": false,
        "title": "Minter"
      }
    },
    "additionalProperties": false,
    "title": "Minter",
    "$id": "minter",
    "class": "query",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "minter": {
      "ui:options": {
        "title": "Query Minter",
        "description": "Retrieve information about the authorized minter of the token. This query will return the minter's details and the maximum token cap, if set."
      }
    }
  },
  "form-data": {}
}
