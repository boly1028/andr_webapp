{
  "schema": {
    "type": "object",
    "required": ["balance"],
    "properties": {
      "balance": {
        "type": "object",
        "required": ["address"],
        "properties": {
          "address": {
            "description": "An address that can be used within the Andromeda ecosystem. Inspired by the cosmwasm-std `Addr` type. This address can be one of two things: 1. A valid human readable address e.g. `cosmos1...` 2. A valid Andromeda VFS path e.g. `/home/user/app/component`",
            "type": "string",
            "$original_type": "AndrAddr",
            "title": "Address"
          }
        },
        "additionalProperties": false,
        "title": "Balance"
      }
    },
    "additionalProperties": false,
    "title": "Balance",
    "$id": "balance",
    "class": "query",
    "version": "0.2.1",
    "classifier": "",
    "description": "Returns the current balance of the given address, 0 if unset. Return type: BalanceResponse."
  },
  "ui-schema": {
    "ui:description":"Check the current balance of a specified address in the Andromeda ecosystem.",
    "balance": {
      "ui:options": {
        "title": "Query Balance",
        "description": "Check the current balance of a specified address in the Andromeda ecosystem."
      },
      "address": {
        "ui:options": {
          "title": "Address",
          "description": "Enter an Andromeda ecosystem address to check its balance.",
          "placeholder": "e.g., andr1fl48v...0eh  or  ./componentName",
          "help": ""
        }
      }
    }
  },
  "form-data": {}
}
