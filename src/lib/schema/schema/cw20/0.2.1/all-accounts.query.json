{
  "schema": {
    "description": "Only with \"enumerable\" extension Returns all accounts that have balances. Supports pagination. Return type: AllAccountsResponse.",
    "type": "object",
    "required": ["all_accounts"],
    "properties": {
      "all_accounts": {
        "type": "object",
        "properties": {
          "limit": {
            "type": ["integer", "null"],
            "format": "uint32",
            "minimum": 0,
            "title": "Limit"
          },
          "start_after": {
            "type": ["string", "null"],
            "title": "Start after"
          }
        },
        "additionalProperties": false,
        "title": "All accounts"
      }
    },
    "additionalProperties": false,
    "title": "All accounts",
    "$id": "all-accounts",
    "class": "query",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "all_accounts": {
      "ui:options ": {
        "title": "Query All Accounts",
        "description": "Retrieve a list of all accounts with balances. Use pagination to manage the results."
      },
      "limit": {
        "ui:options": {
          "title": "Result Limit",
          "description": "Set the maximum number of accounts to be retrieved in one request. Leave blank for default limit.",
          "placeholder": "e.g., 20",
          "help": "Enter a number to limit the number of accounts returned. If left empty, a default limit will be applied."
        }
      },
      "start_after": {
        "ui:options": {
          "title": "Start After Account",
          "description": "Specify an account address to begin listing the next set of accounts in the pagination.",
          "placeholder": "e.g., andr1fl48v...0eh",
          "help": "Enter an account address to start listing accounts after it. Useful for pagination."
        }
      }
    }
  },
  "form-data": {}
}
