{
  "schema": {
    "description": "Returns metadata on the contract - name, decimals, supply, etc. Return type: TokenInfoResponse.",
    "type": "object",
    "required": ["token_info"],
    "properties": {
      "token_info": {
        "type": "object",
        "additionalProperties": false,
        "title": "Token info"
      }
    },
    "additionalProperties": false,
    "title": "Token info",
    "$id": "token-info",
    "class": "query",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "token_info": {
      "ui:options": {
        "title": "Request Token Information",
        "description": "Retrieve detailed information about the token, including its name, supply, and decimal precision."
      }
    }
  },
  "form-data": {}
}
