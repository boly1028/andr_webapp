{
  "schema": {
    "type": "object",
    "required": ["balance"],
    "properties": {
      "balance": {
        "type": "object",
        "required": ["address"],
        "properties": {
          "address": {
            "description": "An address that can be used within the Andromeda ecosystem.",
            "type": "string",
            "$original_type": "AndrAddr",
            "title": "Address"
          }
        },
        "additionalProperties": false,
        "title": "Balance"
      }
    },
    "additionalProperties": false,
    "title": "Balance",
    "$id": "balance",
    "class": "query",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "balance": {
      "address": {
        "ui:widget": "text",
        "ui:title": "Wallet Address",
        "ui:placeholder": "e.g., andr1...5rj",
        "ui:description": "Enter the wallet address for which you want to check the balance."
      }
    }
  },
  "form-data": {}
}
