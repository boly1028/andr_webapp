{
  "schema": {
    "description": "Transfers NFT to buyer and sends funds to seller",
    "type": "object",
    "required": ["buy"],
    "properties": {
      "buy": {
        "type": "object",
        "required": ["token_address", "token_id"],
        "properties": {
          "token_address": { "type": "string", "title": "Token address" },
          "token_id": { "type": "string", "title": "Token id" }
        },
        "additionalProperties": false,
        "title": "Buy"
      }
    },
    "additionalProperties": false,
    "title": "Buy",
    "$id": "buy",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
    "ui-schema": {
      "buy": {
        "ui:options": {
          "title": "Buy NFT",
          "description": "Complete the purchase by transferring NFT to buyer and sending funds to seller."
        },
        "token_address": {
          "ui:widget": "text",
          "ui:title": "Token Address",
          "ui:placeholder": "e.g., andr1...",
          "ui:description": "The address of the NFT contract."
        },
        "token_id": {
          "ui:widget": "text",
          "ui:title": "Token ID",
          "ui:placeholder": "e.g., 123",
          "ui:description": "The unique identifier of the NFT."
        }
      }
    },
    "form-data": {}
  }
  