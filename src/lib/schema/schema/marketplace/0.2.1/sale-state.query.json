{
  "schema": {
    "description": "Gets the sale state for the given sale id.",
    "type": "object",
    "required": ["sale_state"],
    "properties": {
      "sale_state": {
        "type": "object",
        "required": ["sale_id"],
        "properties": {
          "sale_id": {
            "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding.",
            "type": "string",
            "$original_type": "Uint128",
            "title": "Sale id"
          }
        },
        "additionalProperties": false,
        "title": "Sale state"
      }
    },
    "additionalProperties": false,
    "title": "Sale state",
    "$id": "sale-state",
    "class": "query",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "sale_state": {
      "sale_id": {
        "ui:widget": "text",
        "ui:title": "Sale ID",
        "ui:placeholder": "e.g., SaleID123",
        "ui:description": "Enter the sale ID for which you want to retrieve the sale state."
      }
    }
  },
  "form-data": {}
}
