{
  "schema": {
    "description": "Gets the sale ids for the given token.",
    "type": "object",
    "required": ["sale_ids"],
    "properties": {
      "sale_ids": {
        "type": "object",
        "required": ["token_address", "token_id"],
        "properties": {
          "token_address": { "type": "string", "title": "Token address" },
          "token_id": { "type": "string", "title": "Token id" }
        },
        "additionalProperties": false,
        "title": "Sale ids"
      }
    },
    "additionalProperties": false,
    "title": "Sale ids",
    "$id": "sale-ids",
    "class": "query",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {},
  "form-data": {}
}
