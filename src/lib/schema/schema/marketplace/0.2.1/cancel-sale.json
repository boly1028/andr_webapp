{
  "schema": {
    "type": "object",
    "required": ["cancel_sale"],
    "properties": {
      "cancel_sale": {
        "type": "object",
        "required": ["token_address", "token_id"],
        "properties": {
          "token_address": { "type": "string", "title": "Token address" },
          "token_id": { "type": "string", "title": "Token id" }
        },
        "additionalProperties": false,
        "title": "Cancel sale"
      }
    },
    "additionalProperties": false,
    "title": "Cancel sale",
    "$id": "cancel-sale",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "cancel_sale": {
      "ui:options": {
        "title": "Cancel Sale",
        "description": "Cancel an ongoing sale for a token."
      },
      "token_address": {
        "ui:widget": "text",
        "ui:title": "Token Address",
        "ui:placeholder": "e.g., andr1...",
        "ui:description": "The address of the NFT contract."
      },
      "token_id": {
        "ui:widget": "text",
        "ui:title": "Token ID",
        "ui:placeholder": "e.g., 123",
        "ui:description": "The unique identifier of the NFT."
      }
    }
  },
  "form-data": {}
}
