{
  "schema": {
    "description": "Updates the sale's price, demomination, and whitelist",
    "type": "object",
    "required": ["update_sale"],
    "properties": {
      "update_sale": {
        "type": "object",
        "required": ["coin_denom", "price", "token_address", "token_id"],
        "properties": {
          "coin_denom": { "type": "string", "title": "Coin denom" },
          "price": {
            "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
            "type": "string",
            "$original_type": "Uint128",
            "title": "Price"
          },
          "token_address": { "type": "string", "title": "Token address" },
          "token_id": { "type": "string", "title": "Token id" }
        },
        "additionalProperties": false,
        "title": "Update sale"
      }
    },
    "additionalProperties": false,
    "title": "Update sale",
    "$id": "update-sale",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {
    "update_sale": {
      "coin_denom": {
        "ui:widget": "text",
        "ui:title": "Coin Denomination",
        "ui:placeholder": "e.g., uandr",
        "ui:description": "The denomination of the coin used in the sale."
      },
      "price": {
        "ui:widget": "text",
        "ui:title": "Sale Price",
        "ui:placeholder": "e.g., 100000",
        "ui:description": "The price of the item in the sale, in micro-units of the specified coin denomination."
      },
      "token_address": {
        "ui:widget": "text",
        "ui:title": "Token Address",
        "ui:placeholder": "e.g., andr1...5rj",
        "ui:description": "The address of the token being sold."
      },
      "token_id": {
        "ui:widget": "text",
        "ui:title": "Token ID",
        "ui:placeholder": "e.g., UniqueToken123",
        "ui:description": "The unique identifier of the token being sold."
      }
    }
  },
  "form-data": {}
}