{
  "schema": {
    "description": "Release funds all held in Escrow for the given recipient",
    "type": "object",
    "required": ["release_funds"],
    "properties": {
      "release_funds": {
        "type": "object",
        "properties": {
          "limit": {
            "type": ["integer", "null"],
            "format": "uint32",
            "minimum": 0,
            "title": "Limit"
          },
          "recipient_addr": {
            "type": ["string", "null"],
            "title": "Recipient addr"
          },
          "start_after": { "type": ["string", "null"], "title": "Start after" }
        },
        "additionalProperties": false,
        "title": "Release funds"
      }
    },
    "additionalProperties": false,
    "title": "Release funds",
    "$id": "release-funds",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {},
  "form-data": {}
}
