{
  "schema": {
    "description": "Votes on the specified proposal with the specified vote.",
    "type": "object",
    "required": ["vote"],
    "properties": {
      "vote": {
        "type": "object",
        "required": ["proposal_id", "vote"],
        "properties": {
          "proposal_id": {
            "type": "integer",
            "format": "uint64",
            "minimum": 0,
            "title": "Proposal id"
          },
          "vote": {
            "type": "string",
            "enum": ["yes", "no", "abstain", "no_with_veto"],
            "$original_type": "VoteOption",
            "title": "Vote"
          }
        },
        "additionalProperties": false,
        "title": "Vote"
      }
    },
    "additionalProperties": false,
    "title": "Vote",
    "$id": "vote",
    "class": "modifier",
    "version": "0.2.1",
    "classifier": ""
  },
  "ui-schema": {},
  "form-data": {}
}
